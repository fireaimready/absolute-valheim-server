# =============================================================================
# Git Attributes - Ensure consistent line endings
# =============================================================================

# Default to auto-detection
* text=auto

# Shell scripts must have LF line endings (Unix-style)
*.sh text eol=lf
scripts/* text eol=lf

# Dockerfile and compose files
Dockerfile text eol=lf
docker-compose*.yml text eol=lf

# Config files
*.conf text eol=lf
*.env* text eol=lf

# Keep Windows line endings for Windows-specific files
*.ps1 text eol=crlf
*.bat text eol=crlf
*.cmd text eol=crlf
